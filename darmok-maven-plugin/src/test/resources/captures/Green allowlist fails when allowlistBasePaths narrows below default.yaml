# Captured path through claude/mvn/git FSMs for: Green allowlist fails when allowlistBasePaths narrows below default
sequences:
  git: [git-1, git-2, git-3, git-4, git-16, git-17, git-16]
  mvn: [mvn-1, mvn-3, mvn-4, mvn-5]
  claude: [claude-6, claude-17]
