# Captured path through claude/mvn/git FSMs for: A second run on the same day appends to the existing log
sequences:
  git: [git-1, git-1, git-2, git-3, git-4, git-7]
  mvn: [mvn-1, mvn-1, mvn-3, mvn-4, mvn-8]
