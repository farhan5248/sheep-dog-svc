apiVersion: apps/v1
kind: Deployment
metadata:
  name: sheep-dog-mcp-svc
  labels:
    app: sheep-dog
    component: sheep-dog-mcp-svc
spec:
  selector:
    matchLabels:
      component: sheep-dog-mcp-svc
  template:
    metadata:
      labels:
        app: sheep-dog
        component: sheep-dog-mcp-svc
    spec:
      containers:
        - name: sheep-dog-mcp-svc
          image: ghcr.io/farhan5248/sheep-dog-mcp-svc:latest
          imagePullPolicy: "Always"
          env:
            - name: LOG_LEVEL
              value: "INFO"
            - name: LOG_PATH
              value: "/logs"
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: logs-volume
              mountPath: /logs
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "250m"
      volumes:
        - name: logs-volume
          emptyDir: {}
